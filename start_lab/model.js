const Presentation = {
    Slides,
    id,
}

const Slides = {                                //массив слайдов
   slide,
}

const Highlighting = {
    id,
}

const Slide = {
    objectOnSlides,
    background,
}

const _objectOnSlide = {
    object: pictures, share, text
}

const Features = {
    width,
    height,
    x,
    y,
    id
}

const TextBlock = {
    text,
    features,
    font,
    textAlign,
    id
}

const Font = {
    nameFont,
    sizeFont,
    colorFont,
}

const Color = {
    nameColor,
}

const Picture = {
    src,
    features,
    fill,
    stroke,
    id
}

const Shape = {
    typeShapes,
    features,
    design,
    id
}

const TypeShapes = {
    circle,
    square,
    triangle
}

const Design = {
    colorBackground,
    colorStroke,
}